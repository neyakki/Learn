# Общее по Web

## HTTP

HTTP (Hypertext Transport Protocol) - набор правил для передачи различных файлов между клиентом и сервером, работающий поверх TCP/IP.

!!! Warning "TODO"

    Добавить диаграмму взаимодействия

Коммуникация между клиентом и сервером выполняется с помощью цикла запрос-ответ:

1. Клиент инициализирует запрос к серверу;
2. Сервер обрабатывает полученный запрос;
3. Сервер отправляет ответ клиенту;
4. Клиент получает ответ и обрабатывает его;
5. Клиент может продолжать инициировать соединение с севером.

!!! Note "URI, URL, URN"

    * URI – имя и адрес ресурса в сети, включает в себя URL и URN:
    * URL – адрес ресурса в сети, определяет местонахождение и способ обращения к нему:
    * URN – имя ресурса в сети, определяет только название ресурса, но не говорит как к нему подключиться:

HTTP запрос

```http
GET /api/data HTTP/1.1

Host: example.com
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/89.0.4389.82 Safari/537.36
Accept: application/json
Accept-Language: en-US,en;q=0.5
Authorization: Token abc123
Cache-Control: no-cache
Connection: keep-alive
Referer: https://www.google.com/
Pragma: no-cache

body message
```

Где:

-   `GET` - метод запроса. Может быть следующим:

    -   `GET` - запрашивает данные с указанного ресурса;
    -   `POST` - позволяет изменять состояние на сервере;
    -   `PUT` - заменяет данные;
    -   `PATCH` - применяет частичные изменения;
    -   `DELETE` - удаляет специализированный ресурс;
    -   `HEAD` - аналогичен методу `GET`, только без тела запроса;

-   `/api/data` - URL запрашиваемого ресурса;
-   `HTTP/1.1` - используемая версия протокола.

-   `body message` - тело запроса. Не содержится у методов: `GET`, `DELETE`, `HEAD`.

Все что не описано, является заголовками запроса. Заголовки - дополнительная информация отправляемая между клиентом и сервером. [Список](https://developer.mozilla.org/ru/docs/Web/HTTP/Headers) всех заголовков.

Сервер в ответ на запрос посылает HTTP-ответ.

```http
HTTP/1.1 200 OK
Date: Sun, 28 Mar 2023 10:15:00 GMT
Content-Type: application/json
Server: Apache/2.4.39 (Unix) OpenSSL/1.1.1c PHP/7.3.6
Content-Length: 1024

{
    "name": "John Doe",
    "email": "johndoe@example.com",
    "age": 30,
    "address": {
        "street": "123 Main St",
        "city": "Anytown",
        "state": "CA",
        "zip": "12345"
    }
}
```

HTTP-ответ отличается только начальной строкой, в ней он не содержит HTTP-метода, а содержит код статуса.

Коды статуса:

-   1xx - Информационные;
-   2xx - Успешные;
-   3xx - Перенаправление;
-   4xx - Ошибки клиента;
-   5xx - Ошибки сервера

## HTTPS

HTTPS - зашифрованная версия HTTP, использующая SSL или TLS. SSL/TLS-сертификат ― это цифровая подпись сайта.

Функции протокола HTTPS:

-   Шифрование - информация передаётся в зашифрованном виде, поэтому злоумышленники не могут украсть информацию, которой обмениваются посетители сайта, а также отследить их действия на других страницах;
-   Аутентификация - посетители уверены, что переходят на официальный сайт компании, а не на дубликат, сделанный злоумышленником;
-   Сохранение данных - протокол фиксирует все изменения данных. Если злоумышленник всё-таки пытался взломать защиту, об этом можно узнать из сохранённых данных.

Шифрование HTTPS происходит при помощи симметричного и асимметричного ключа:

-   Асимметричный ключ — каждая сторона имеет два ключа: публичный и частный. Публичный ключ доступен любому. Частный известен только владельцу. Если браузер хочет отправить сообщение, то он находит публичный ключ сервера, шифрует сообщение и отправляет на сервер. Далее сервер расшифровывает полученное сообщение с помощью своего частного ключа. Чтобы ответить пользователю, сервер делает те же самые действия: поиск публичного ключа собеседника, шифрование, отправка;
-   Симметричный ключ — у обеих сторон есть один ключ, с помощью которого они передают данные. Между двумя сторонами уже должен быть установлен первичный контакт, чтобы браузер и сервер знали, на каком языке общаться.
