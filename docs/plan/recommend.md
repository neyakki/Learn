# Рекомендации по обучению

1. Верим в себя, поддерживаем морально и делаем. Дорогу осилит идущий.
2. Всегда придерживаемся правила 20% теории, 80% практики. Без исключений. Нужно писать код руками постоянно, вырабатывать привычку, привыкать к коду и инструментам. Если непонятно какую практику делать - найдите в интернете готовое или постарайтесь придумать задачу сами. (сам долго преодолевал боязнь чистого листа и вот это все)
3. Не пишем идеальный код, мы просто учимся. Ошибки - это хорошо и полезно. Читаем описание ошибок и пытаемся понять в чем корень проблемы (полезный навык). Учимся пользоваться дебаггером для борьбы с ошибками (еще один полезный навык)
4. Тему стоит разбирать 2 недели (спринт). Первую неделю мы смотрим youtube, читаем статьи, перепечатываем за автором и запускаем, смотрим как работает. Вторую неделю пробуем сами. Можно дать допольнительную неделю на теорию, но лучше не зависать на теме - их много, а времени мало. К следующей теме приступаем, когда уверенно чувствуем текущую и можем повторить код (пусть и с ошибками, но концептуально верно)
5. Старайтесь кайфовать и пишите код в свое удовольствие. Шутите и играйте с кодом. Не старайсь сразу писать сухой бизнес код для мужчин в костюмах. Have fun!

Наткнулся на [видео](https://www.youtube.com/watch?v=uOa7XtkK1vQ) от автора Alek OS, решил сохранить для себя

-   Командная строка + базы данных (cmd + sql);
-   Собственная программа;
-   Фреймворки (Django / React);
-   Алгоритм BIG O;
-   Базовые структуры данных (массивы, связанные списки, хэш-таблицы, стек, очередь);
-   Системы счисления (Шестнадцатиричная сс);
-   Архитектура компьютера;
-   Битовые операции;
-   Числа с плавающей точкой;
-   Строки (ASCII, UNICODE, кодировки);
-   Язык ассемблера;
-   Стек (передача параметров, кадры функции, возврат);
-   Рекурсия;
-   Сортировка (Пузырьком, вставкой, кучей - Хоара, Ломуто);
-   Поиск (Вставка, поиск, удаление узлов);
-   Интернет (TCP/IP - Прикладной уровень, сетевой уровень, канальный уровень);
-   Операционные системы (процесс, поток);
-   Файловые системы (FAT32, NTFS, EXT3);
-   Исполняемые файлы;
-   Сжатие (с потерями и без);
-   Криптография (https);
-   3D графика (Шейдеры, Алгоритмы, Линейная алгебра);
-   Компилятор (Лексический анализ, Синтаксический анализ, Семантический анализ, AST, генерация машинного кода);
-   Математика и алгоритмы

## Языки и утилиты

Изучить я хочу 4 (+2) языка - _python_, _javascript_, _go_, _rust_, _c_, _assembler_.

-   Python:

    -   Фреймворки и библиотеки:

        -   FastAPI;
        -   Pydantic;
        -   SQLAlchemy;
        -   Django / DRF;
        -   Alembic;
        -   Prompt;
        -   Cookiecutter;

    -   Утилиты:

        -   Pytest;
        -   Mypy;
        -   poetry;
        -   ruff;
        -   pre-commit;
        -   invoke;
        -   mkdocs

-   JavaScript:

    -   Фреймворки и библиотеки:

        -   Vue;
        -   React;
        -   d3.js
        -   flatpickr

    -   Утилиты:

        -   npm;
        -   vite;
        -   TypeScript;
        -   ESlint;
        -   Husky;
        -   Mocha;
        -   Jest;
        -   TestCafe;
        -   Istanbul;
        -   esdocs;
        -   babel;
        -   emmet

-   CSS / SCSS
-   Tailwind
